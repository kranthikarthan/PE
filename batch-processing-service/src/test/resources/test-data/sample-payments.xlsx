# This would be an Excel file in a real scenario
# For testing purposes, we'll create a simple Excel file with payment data
# The actual Excel file would contain the same data as the CSV file above
# but in Excel format (.xlsx)

# Note: In a real implementation, this would be a binary Excel file
# For testing, we'll use the CSV data as a reference
